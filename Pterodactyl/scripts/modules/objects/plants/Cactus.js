import { ObjHelper } from "../ObjHelper.js";

export class Background extends ObjHelper {
	constructor(sx, sy, width, height, dx, dy, dWidth, dHeight) {
		super(sx, sy, width, height, dx, dy, dWidth, dHeight);
	}
}
